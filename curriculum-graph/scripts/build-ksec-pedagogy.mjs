/* กลยุทธ์การสอนและวิธีประเมินรายข้อ KSA + รายวิชาแกน

   สองชั้นที่ต้องแยกให้ชัด
     · รายวิชาแกน  = DERIVED จากข้อมูล CLO จริง (ดู pickAnchors) ไม่ใช่การเลือกด้วยมือ
     · กลยุทธ์/การประเมิน = AUTHORED ข้อเสนอที่ยังต้องให้คณะกรรมการหลักสูตรรับรอง

   Run: npm run build:ksa (chained)                                                       */
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { KNOWLEDGE, SKILLS_KSEC, ETHICS, CHARACTER } from "../src/ksecData.js";
import { COURSE_KSEC } from "../src/courseKsecData.js";
import { CLO_LIST } from "../src/cloData.js";
import { COURSES } from "../src/data.js";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..", "..");
const OUT_MD = path.join(root, "Labor_Growth_Report_Vault/05_TQF2_Academic_Drafts/20_KSEC_Teaching_and_Assessment.md");
const OUT_JS = path.join(root, "curriculum-graph/src/ksecPedagogyData.js");

const LV = { M: 3, R: 2, I: 1 };
/* วิชาบูรณาการปลายทาง — อ้าง KSA เกือบทุกตัวจึงไม่ใช่ "เจ้าของเนื้อหา" ของรหัสใด */
const INTEGRATIVE = new Set(["EN-714-12019", "EN-714-12020", "EN-714-17001", "EN-714-17002"]);
const courseOf = c => COURSES.find(x => x.c === c);
const titleOf = c => courseOf(c)?.t || COURSE_KSEC[c]?.name || c;

/* ── DERIVED: รายวิชาแกนของแต่ละรหัส ──
   เกณฑ์ตามลำดับ: ไม่ใช่วิชาบูรณาการปลายทาง → วิชาที่อ้าง KSA น้อยกว่า (เจาะจงกว่า)
   → เปิดสอนก่อน (เป็นผู้แนะนำเนื้อหา) → ระดับ CLO สูงกว่า                              */
const usage = {};
Object.values(COURSE_KSEC).filter(c => !c.derivedFrom).forEach(c => {
  const entry = CLO_LIST.find(e => e.c === c.code);
  const breadth = c.K.length + c.S.length + c.E.length + c.C.length;
  c.clos.forEach(k => {
    const clo = entry?.clos.find(x => x.n === k.n);
    const lv = clo ? Math.max(...clo.plo.map(([, l]) => LV[l] || 1)) : 1;
    [...k.K, ...k.S, ...k.E, ...k.C].forEach(code => {
      const g = (usage[code] = usage[code] || {});
      const row = (g[c.code] = g[c.code] || { c: c.code, breadth, lv: 0, clos: [], sem: courseOf(c.code)?.sem ?? 99 });
      row.lv = Math.max(row.lv, lv);
      row.clos.push(k.n);
    });
  });
});

function pickAnchors(code) {
  const rows = Object.values(usage[code] || {});
  if (!rows.length) return [];
  const core = rows.filter(r => !INTEGRATIVE.has(r.c));
  const pool = core.length ? core : rows;
  return pool
    .sort((a, b) => a.breadth - b.breadth || a.sem - b.sem || b.lv - a.lv || a.c.localeCompare(b.c))
    .slice(0, 2)
    .map(r => ({ c: r.c, t: titleOf(r.c), clos: [...new Set(r.clos)].sort((x, y) => x - y), lv: r.lv }));
}

/* ── AUTHORED: รูปแบบการสอนและการประเมินตามลักษณะของรหัส ──
   ตั้งค่าเริ่มต้นตามมิติ แล้วเขียนทับเฉพาะรหัสที่มีลักษณะเฉพาะจริง ๆ                    */
const BASE = {
  K: { teach: "การสอนโดยตรง (Direct Instruction)",
       how: "บรรยายเชิงโต้ตอบพร้อมตัวอย่างคำนวณ · มอบหมายแบบฝึกหัดรายสัปดาห์ · สรุปหลักการด้วยผังมโนทัศน์",
       assess: "ข้อสอบกลางภาค/ปลายภาค และแบบฝึกหัดที่ตรวจตามเกณฑ์",
       artifact: "กระดาษคำตอบและสมุดแบบฝึกหัดที่แสดงขั้นตอนการคิด" },
  S: { teach: "การเรียนรู้ผ่านประสบการณ์ (Experiential Learning)",
       how: "สาธิตแล้วให้ลงมือทำในห้องปฏิบัติการ · มอบหมายชิ้นงานที่ต้องส่งมอบใช้งานได้ · ตรวจงานพร้อมให้ข้อมูลป้อนกลับเป็นรอบ",
       assess: "ประเมินชิ้นงานตามเกณฑ์การให้คะแนนเชิงสมรรถนะ (Performance Rubric)",
       artifact: "ชิ้นงานหรือระบบที่ทำงานได้จริง พร้อมบันทึกกระบวนการ" },
  E: { teach: "การสอนเชิงปฏิสัมพันธ์ (Interactive Instruction)",
       how: "อภิปรายกรณีศึกษา · สะท้อนคิดหลังปฏิบัติงาน · ผู้สอนแสดงแบบอย่างพฤติกรรมที่คาดหวัง",
       assess: "เกณฑ์ให้คะแนนเชิงพฤติกรรม ประกอบการประเมินตนเองและโดยเพื่อนร่วมทีม",
       artifact: "หลักฐานเชิงพฤติกรรมตามที่กำหนดในสมุดรหัส KSEC" },
  C: { teach: "การเรียนรู้ผ่านการสะท้อนผล (Reflective Learning)",
       how: "กำหนดข้อตกลงพฤติกรรมตั้งแต่ต้นภาค · ให้ข้อมูลป้อนกลับเชิงพฤติกรรมทุกรอบส่งงาน · ให้ผู้เรียนสะท้อนผลตนเองเทียบกับหลักฐาน",
       assess: "เกณฑ์เชิงพฤติกรรมที่วัดความสม่ำเสมอตลอดภาค ร่วมกับการประเมินตนเองและการประเมินโดยเพื่อน",
       artifact: "แฟ้มสะท้อนผลและบันทึกพฤติกรรมที่สังเกตได้ตลอดภาคการศึกษา" }
};

const OVERRIDE = {
  /* ความรู้ที่ต้องเห็นของจริงจึงเข้าใจ */
  K2:  { teach: "การสอนโดยตรงร่วมกับปฏิบัติการ", how: "บรรยายทฤษฎีแล้วทดสอบวัสดุจริงในห้องปฏิบัติการเพื่อยืนยันผลการคำนวณ", assess: "ข้อสอบคำนวณ ร่วมกับรายงานผลทดสอบวัสดุ", artifact: "รายงานการคำนวณและผลทดสอบที่เทียบกับทฤษฎี" },
  K3:  { teach: "การสอนโดยตรงร่วมกับปฏิบัติการ", how: "บรรยายสมดุลมวล–พลังงาน แล้ววัดจริงจากชุดทดลองความร้อน–ของไหล", assess: "ข้อสอบคำนวณ ร่วมกับรายงานสมดุลพลังงานจากข้อมูลที่วัดเอง", artifact: "รายงานสมดุลมวล–พลังงานพร้อมค่าความคลาดเคลื่อน" },
  K4:  { teach: "การสอนโดยตรงร่วมกับปฏิบัติการ", how: "บรรยายทฤษฎีการวัด แล้วสอบเทียบเครื่องมือจริงและวิเคราะห์ความไม่แน่นอน", assess: "ข้อสอบ ร่วมกับใบรายงานผลการสอบเทียบ", artifact: "ใบสอบเทียบและรายงานความไม่แน่นอนของการวัด" },
  K17: { teach: "การสอนโดยตรงร่วมกับการฝึกปฏิบัติ", how: "สาธิตมาตรฐานการเขียนแบบ แล้วให้เขียนแบบตามโจทย์และตรวจแบบร่วมกัน", assess: "ตรวจแบบตามเกณฑ์มาตรฐานการเขียนแบบ", artifact: "ชุดแบบที่ผ่านการตรวจและแก้ไขตามข้อทักท้วง" },
  K23: { teach: "การสอนเชิงปฏิสัมพันธ์ (Interactive Instruction)", how: "อภิปรายกรณีศึกษาจริยธรรมและกรณีระบบล้มเหลว · เทียบมาตรฐานสากลกับบริบทไทย", assess: "ข้อสอบอัตนัยเชิงวิเคราะห์กรณี ร่วมกับรายงานประเมินความสอดคล้องตามมาตรฐาน", artifact: "รายงานวิเคราะห์กรณีและตารางเทียบมาตรฐาน" },
  K25: { teach: "การสอนเชิงปฏิสัมพันธ์ (Interactive Instruction)", how: "วิเคราะห์ตัวอย่างงานเขียนที่ดีและไม่ดี · ฝึกเขียนแล้ววิจารณ์โดยเพื่อน", assess: "ประเมินรายงานและการนำเสนอตามเกณฑ์การสื่อสาร", artifact: "รายงานเชิงเทคนิคและสไลด์นำเสนอทั้งภาษาไทยและอังกฤษ" },
  K26: { teach: "การเรียนรู้ผ่านประสบการณ์ (Experiential Learning)", how: "ทำงานกลุ่มจริงพร้อมข้อตกลงทีม · ใช้กระดานงานแบบ Agile ติดตามความคืบหน้า", assess: "ประเมินกระบวนการทำงานของทีมและแผนโครงการ", artifact: "ข้อตกลงทีม แผนงาน และบันทึกการทบทวนย้อนหลัง" },

  /* ทักษะที่ต้องใช้เครื่องมือ/สนามจริง */
  S3:  { teach: "การเรียนรู้ผ่านประสบการณ์ในห้องปฏิบัติการ", how: "คำนวณออกแบบ แล้วสร้างและทดสอบชิ้นงานจริงเพื่อเทียบกับผลคำนวณ", assess: "ประเมินชิ้นงานที่สร้างได้จริงร่วมกับความถูกต้องของการคำนวณ", artifact: "ชิ้นงานที่ประกอบเสร็จพร้อมรายงานเทียบผลคำนวณกับผลทดสอบ" },
  S14: { teach: "การฝึกปฏิบัติแบบมีผู้ชี้แนะ (Guided Practice)", how: "เขียนแบบตามโจทย์เป็นรอบ ตรวจแบบร่วมกัน แล้วแก้ไขตามข้อทักท้วง", assess: "ตรวจแบบตามเกณฑ์มาตรฐาน พร้อมตรวจการควบคุมเวอร์ชันของแบบ", artifact: "ชุดแบบพร้อมประวัติการแก้ไขที่สอบย้อนกลับได้" },
  S15: { teach: "การเรียนรู้ผ่านประสบการณ์ในห้องปฏิบัติการ", how: "ต่อวงจรและระบบขับเคลื่อนจริง สอบเทียบ แล้วทดสอบหน้าที่ความปลอดภัย", assess: "ประเมินการปฏิบัติงานจริงร่วมกับผลการสอบเทียบและการทดสอบอินเตอร์ล็อก", artifact: "ระบบที่ต่อเสร็จ ใบสอบเทียบ และผลการทดสอบความปลอดภัย" },
  S16: { teach: "การเรียนรู้ผ่านโครงงาน (Project-Based Learning)", how: "บูรณาการอุปกรณ์ควบคุมกับระบบตรวจวัดเป็นสถานีทำงานจริงตามโจทย์", assess: "ประเมินระบบที่บูรณาการได้จริงร่วมกับการทดสอบความน่าเชื่อถือ", artifact: "สถานีระบบที่ทำงานต่อเนื่องได้พร้อมบันทึกผลการทดสอบ" },
  S18: { teach: "การสอนเชิงปฏิสัมพันธ์ร่วมกับการทำเอกสารจริง", how: "วิเคราะห์กรณีความเสี่ยง แล้วจัดทำทะเบียนความเสี่ยงและเอกสารธรรมาภิบาลของงานตนเอง", assess: "ตรวจเอกสารธรรมาภิบาลตามรายการตรวจสอบ และสอบทานการทดสอบก่อนส่งมอบ", artifact: "ทะเบียนความเสี่ยง เอกสารกำกับโมเดล และผลการทดสอบก่อนใช้งาน" },
  S19: { teach: "การสอนเชิงปฏิสัมพันธ์ (Interactive Instruction)", how: "ฝึกนำเสนอต่อผู้ฟังต่างกลุ่ม · สัมภาษณ์ผู้ใช้จริงเพื่อเก็บข้อกำหนด", assess: "ประเมินการนำเสนอและเอกสารข้อกำหนดที่มีเกณฑ์การยอมรับ", artifact: "เอกสารข้อกำหนดพร้อมเกณฑ์การยอมรับ และบันทึกการนำเสนอ" },
  S20: { teach: "การเรียนรู้ผ่านโครงงานแบบทีม", how: "ทำงานเป็นรอบสั้นพร้อมทบทวนย้อนหลัง · หมุนเวียนบทบาทในทีม", assess: "ประเมินการมีส่วนร่วมรายบุคคลร่วมกับการประเมินโดยเพื่อนร่วมทีม", artifact: "บันทึกการมีส่วนร่วม ผลการทบทวนย้อนหลัง และแฟ้มสะท้อนผล" },

  /* จริยธรรมและลักษณะบุคคลที่ต้องใช้วิธีเฉพาะ */
  E2:  { teach: "การสอนทางอ้อมผ่านการตรวจงานซ้ำ", how: "กำหนดให้ส่งข้อมูลดิบและโค้ดที่รันซ้ำได้ · ให้รายงานผลที่ล้มเหลวด้วย", assess: "ตรวจความสามารถในการทำซ้ำผลโดยผู้อื่น และตรวจการรายงานผลเชิงลบ", artifact: "ที่เก็บโค้ดและข้อมูลที่ผู้อื่นรันซ้ำได้ พร้อมบันทึกเวอร์ชัน" },
  E1:  { teach: "การสอนเชิงปฏิสัมพันธ์ผ่านกรณีศึกษาจริยธรรม", how: "อภิปรายสถานการณ์ที่ต้องเลือกระหว่างผลประโยชน์กับความถูกต้อง · ผู้สอนแสดงจุดยืนเป็นแบบอย่าง", assess: "เกณฑ์พฤติกรรมด้านความซื่อสัตย์ ร่วมกับผลประเมินจากอาจารย์นิเทศหรือสถานประกอบการ", artifact: "การอ้างอิงครบถ้วน การเปิดเผยการใช้เครื่องมือ AI และบันทึกการทักท้วง" },
  C5:  { teach: "การเรียนรู้ผ่านโครงงานแบบทีม", how: "กำหนดบทบาทและข้อตกลงทีมตั้งแต่ต้น · ทบทวนพฤติกรรมทีมทุกรอบส่งงาน", assess: "การประเมินโดยเพื่อนร่วมทีมและการประเมินแบบ 360 องศา", artifact: "บันทึกการมีส่วนร่วมรายบุคคลและผลประเมินโดยเพื่อน" },
  C7:  { teach: "การเรียนรู้ด้วยตนเอง (Independent Study)", how: "ให้เลือกเทคโนโลยีใหม่มาศึกษาเองและนำเสนอ · จัดทำแผนพัฒนาตนเอง", assess: "ประเมินแฟ้มสะสมการเรียนรู้และแผนพัฒนาตนเอง", artifact: "แฟ้มสะท้อนผล แผนการเรียนรู้ และหลักฐานการทดลองเทคโนโลยีใหม่" }
};

const DIM = { K: "Knowledge", S: "Skill", E: "Ethics", C: "Character" };
const all = [
  ...KNOWLEDGE.map(r => ({ ...r, dim: "K" })),
  ...SKILLS_KSEC.map(r => ({ ...r, dim: "S" })),
  ...ETHICS.map(r => ({ ...r, dim: "E" })),
  ...CHARACTER.map(r => ({ ...r, dim: "C" }))
];

const rows = all.map(r => {
  const base = BASE[r.dim];
  const o = OVERRIDE[r.id] || {};
  return {
    id: r.id, dim: r.dim, name: r.name, plo: r.plo,
    teach: o.teach || base.teach,
    how: o.how || base.how,
    assess: o.assess || base.assess,
    artifact: o.artifact || base.artifact,
    tailored: Boolean(OVERRIDE[r.id]),
    anchors: pickAnchors(r.id)
  };
});

const noAnchor = rows.filter(r => !r.anchors.length);
if (noAnchor.length) console.log("  !! ไม่มีรายวิชาแกน:", noAnchor.map(r => r.id).join(", "));

/* ── เอกสารในวอลต์ ── */
const table = list => [
  "| รหัส | หัวข้อ | กลยุทธ์การสอน | วิธีดำเนินการ | วิธีประเมิน | หลักฐาน | รายวิชาแกน |",
  "|---|---|---|---|---|---|---|",
  ...list.map(r => `| **${r.id}** | ${r.name} | ${r.teach} | ${r.how} | ${r.assess} | ${r.artifact} | ${
    r.anchors.map(a => `\`${a.c}\`<br><small>${a.t}</small>`).join("<br>") || "—"} |`)
].join("\n");

const doc = `# กลยุทธ์การสอนและวิธีประเมินรายข้อ KSA

> กำหนด **วิธีสอน · วิธีประเมิน · หลักฐาน · รายวิชาแกน** ให้ครบทั้ง ${rows.length} รหัสจาก [[18_KSEC_Codebook|สมุดรหัส KSA]]
> เพื่อให้ผู้รับผิดชอบรายวิชานำไปเขียน มคอ.3 และออกแบบเกณฑ์ประเมินได้ทันที
>
> **สร้างอัตโนมัติ** จาก \`curriculum-graph/scripts/build-ksec-pedagogy.mjs\` · สั่งสร้างใหม่ด้วย \`npm run build:ksa\`

> [!warning] แยกให้ชัดว่าส่วนใดมาจากข้อมูล ส่วนใดเป็นข้อเสนอ
> | คอลัมน์ | สถานะ |
> |---|---|
> | **รายวิชาแกน** | **มาจากข้อมูลจริง** — คำนวณจาก CLO ที่อ้างรหัสนั้น โดยตัดวิชาบูรณาการปลายทาง (โครงงาน/สหกิจ) ออก เพราะอ้าง KSA เกือบทุกตัวจึงไม่ใช่เจ้าของเนื้อหา แล้วเรียงตามความเจาะจงของรายวิชาและภาคที่เปิดสอน |
> | **กลยุทธ์การสอน · วิธีดำเนินการ · วิธีประเมิน · หลักฐาน** | **เป็นข้อเสนอที่ผู้จัดทำออกแบบขึ้น** ยังไม่ผ่านการรับรองจากคณะกรรมการหลักสูตร ต้องให้ผู้รับผิดชอบรายวิชาตรวจและปรับก่อนใช้จริง |

**สรุป:** ปรับเฉพาะรายข้อ ${rows.filter(r => r.tailored).length} รหัส · ใช้รูปแบบตามมิติ ${rows.filter(r => !r.tailored).length} รหัส · ทุกรหัสมีรายวิชาแกน ${rows.every(r => r.anchors.length) ? "ครบ" : "ไม่ครบ"}

---

## 1. ความรู้ (Knowledge · K1–K${KNOWLEDGE.length})

รูปแบบตั้งต้น: **${BASE.K.teach}** — ประเมินด้วยข้อสอบและแบบฝึกหัด ยกเว้นรหัสที่ต้องเห็นของจริงจึงเข้าใจ ซึ่งเสริมปฏิบัติการเข้าไป

${table(rows.filter(r => r.dim === "K"))}

---

## 2. ทักษะ (Skill · S1–S${SKILLS_KSEC.length})

รูปแบบตั้งต้น: **${BASE.S.teach}** — ประเมินจากชิ้นงานที่ทำได้จริง ไม่ใช่ข้อสอบ

${table(rows.filter(r => r.dim === "S"))}

---

## 3. จริยธรรม (Ethics · E1–E${ETHICS.length})

รูปแบบตั้งต้น: **${BASE.E.teach}** — ประเมินด้วยเกณฑ์เชิงพฤติกรรมและหลักฐานที่กำหนดไว้ในสมุดรหัส **ห้ามให้คะแนนจากความประทับใจ**

${table(rows.filter(r => r.dim === "E"))}

---

## 3ก. ลักษณะบุคคล (Character · C1–C${CHARACTER.length})

รูปแบบตั้งต้น: **${BASE.C.teach}** — ประเมินจาก **ความสม่ำเสมอตลอดภาคการศึกษา** ไม่ใช่จากชิ้นงานเดียว

${table(rows.filter(r => r.dim === "C"))}

---

## 4. รายวิชาแกนที่ถูกอ้างถึงมากที่สุด

รายวิชาที่เป็นแกนของหลายรหัส คือจุดที่ควรลงทุนด้านเกณฑ์ประเมินและทรัพยากรก่อน

| รายวิชา | ชื่อ | จำนวนรหัสที่เป็นแกน | รหัส |
|---|---|--:|---|
${Object.entries(rows.flatMap(r => r.anchors.map(a => [a.c, r.id]))
  .reduce((m, [c, id]) => ((m[c] = m[c] || []).push(id), m), {}))
  .sort((a, b) => b[1].length - a[1].length)
  .slice(0, 12)
  .map(([c, ids]) => `| \`${c}\` | ${titleOf(c)} | ${ids.length} | ${ids.join(", ")} |`).join("\n")}

---

[[18_KSEC_Codebook|← สมุดรหัส KSA]] | [[19_Course_and_CLO_KSEC_Tables|ตารางรหัส KSA รายวิชาและราย CLO]] | [[../08_TQF2_Book_Revisions/20_Teaching_and_Assessment_by_PLO|กระบวนการเรียนการสอนรายข้อ PLO]]
`;

fs.writeFileSync(OUT_MD, doc, "utf8");
fs.writeFileSync(OUT_JS,
  `/* AUTO-GENERATED by scripts/build-ksec-pedagogy.mjs — do not edit by hand.
   anchors = derived from CLO data · teach/assess = authored proposal, pending committee approval
   Regenerate with: npm run build:ksa */\n\n` +
  `export const KSEC_PEDAGOGY = ${JSON.stringify(rows, null, 1)};\n\n` +
  `export const KSEC_PEDAGOGY_BY_ID = Object.fromEntries(KSEC_PEDAGOGY.map(r => [r.id, r]));\n`,
  "utf8");

console.log(`20_KSEC_Teaching_and_Assessment.md + ksecPedagogyData.js: ${rows.length} รหัส · ปรับเฉพาะราย ${rows.filter(r => r.tailored).length} · มีแกนครบ ${rows.filter(r => r.anchors.length).length}/${rows.length}`);
