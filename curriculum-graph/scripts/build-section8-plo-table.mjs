/* สร้างตารางที่ 8.3ก ในหมวดที่ 8 จากตารางที่ 6.1 ในหมวดที่ 6 โดยตรง
   เพื่อให้สองหมวดพูดตรงกันเสมอ — วิธีวัด เกณฑ์ประเมิน และระยะเวลา ใช้ค่าเดียวกัน
   ต่างกันเพียง 8.3ก เพิ่มคอลัมน์ "เครื่องมือ" และไม่มีคอลัมน์น้ำหนักกับผู้ประเมิน
   สั่งสร้างใหม่ด้วย: npm run build:section8 */
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../..");
const S6 = path.join(root, "Labor_Growth_Report_Vault/08_TQF2_Book_Revisions/12_Section6_Revised.md");
const S8 = path.join(root, "Labor_Growth_Report_Vault/08_TQF2_Book_Revisions/10_Section8_Revised.md");

const cells = row => row.split("|").map(c => c.trim());
const clean = t => (t || "").replaceAll("<br>", " · ").replace(/\*\*/g, "").trim();

/* ข้อความผลลัพธ์การเรียนรู้ฉบับเต็ม ใช้ในคอลัมน์ที่สองของ 8.3ก */
const PLO_TEXT = {
  1: "ระบุและแก้ไขปัญหาทางวิศวกรรมที่ซับซ้อนในภาคการเกษตร อุตสาหกรรม หรือองค์กร โดยบูรณาการองค์ความรู้ทางวิทยาศาสตร์และวิศวกรรมศาสตร์",
  2: "ออกแบบและพัฒนาระบบปัญญาประดิษฐ์และระบบควบคุมที่ตอบสนองการใช้งานจริง โดยยึดหลักความปลอดภัย มาตรฐานสากล และความยั่งยืน",
  3: "สื่อสารสารสนเทศทางเทคนิคและคุณค่าของระบบปัญญาประดิษฐ์ผ่านสื่อที่เหมาะสม แก่ผู้รับสารทั้งด้านเทคนิคและผู้ตัดสินใจ",
  4: "ปฏิบัติงานตามหลักจริยธรรมวิชาชีพและธรรมาภิบาลปัญญาประดิษฐ์ โดยตระหนักถึงข้อกฎหมาย ความมั่นคงปลอดภัย และผลกระทบต่อสังคม",
  5: "ปฏิบัติงานร่วมกับทีมสหวิทยาการได้ทั้งบทบาทผู้นำและสมาชิก เพื่อบริหารจัดการโครงการและส่งมอบผลงานตามแผน",
  6: "วิเคราะห์และประเมินสมรรถนะของระบบผ่านกระบวนการทดลอง โดยประยุกต์หลักวิทยาการข้อมูลและวิจารณญาณทางวิศวกรรม",
  7: "ประยุกต์ใช้เทคโนโลยีและมาตรฐานใหม่ผ่านการเรียนรู้ด้วยตนเองอย่างต่อเนื่อง เพื่อสร้างสรรค์นวัตกรรมที่ส่งมอบคุณค่าทางเศรษฐกิจและสังคม",
};
const PLO_NAME = {
  1: "การแก้ปัญหาทางวิศวกรรม",
  2: "การออกแบบระบบปัญญาประดิษฐ์และระบบควบคุม",
  3: "การสื่อสารสารสนเทศทางเทคนิค",
  4: "จริยธรรมและธรรมาภิบาลปัญญาประดิษฐ์",
  5: "การทำงานเป็นทีมและการบริหารโครงการ",
  6: "การทดลองและประเมินสมรรถนะระบบ",
  7: "การเรียนรู้ตลอดชีวิตและนวัตกรรมยั่งยืน",
};

/* เครื่องมือที่ใช้กับวิธีวัดแต่ละตระกูล — คอลัมน์นี้เป็นสิ่งที่ 6.1 ไม่มี จึงกำหนดที่นี่ */
const TOOLS = [
  [/สอบข้อเขียน/, "ข้อสอบอัตนัยและปรนัย พร้อมเกณฑ์การให้คะแนนรายข้อ"],
  [/งานที่มอบหมายและกรณีศึกษา/, "ใบงานกรณีศึกษา และรูบริกการวิเคราะห์ปัญหา"],
  [/ผลการทดสอบ/, "แผนการทดสอบและรายการเกณฑ์การยอมรับ"],
  [/ชิ้นงานเอกสาร/, "รายการตรวจสอบธรรมาภิบาล และแบบตรวจเอกสารกำกับโมเดล"],
  [/ชิ้นงานสื่อสาร/, "รูบริกแผงสรุปข้อมูลและเอกสารส่งมอบ"],
  [/ชิ้นงานหลักฐาน/, "รูบริกแฟ้มหลักฐานการทดลอง และรายการตรวจสอบการทำซ้ำ"],
  [/ชิ้นงานนวัตกรรม/, "รูบริกประเมินคุณค่าและตัวชี้วัดบีซีจี"],
  [/ชิ้นงาน/, "รูบริกแยกองค์ประกอบ และแบบตรวจรับชิ้นงาน"],
  [/การนำเสนอปากเปล่า/, "รูบริกการนำเสนอและการตอบข้อซักถาม"],
  [/จากรายงาน/, "รูบริกรายงานเชิงเทคนิค ทั้งด้านเนื้อหาและการใช้ภาษา"],
  [/พฤติกรรมและการสังเกต/, "แบบสังเกตพฤติกรรม และบันทึกเหตุการณ์สำคัญ"],
  [/เพื่อนร่วมทีม/, "แบบประเมิน 360 องศา และแบบตรวจทานผลงานร่วมกัน"],
  [/แฟ้มสะสมการเรียนรู้/, "รูบริกแฟ้มสะสมการเรียนรู้ดิจิทัล"],
  [/การสะท้อนคิด/, "แบบบันทึกสะท้อนคิด และแบบแผนพัฒนาตนเอง"],
  [/การปฏิบัติงานจริง/, "แบบประเมินสหกิจศึกษาของสถานประกอบการ และแบบบันทึกการนิเทศ"],
  [/การปฏิบัติ/, "แบบสังเกตการปฏิบัติ และรายการตรวจสอบความปลอดภัย"],
];
const toolOf = method => (TOOLS.find(([re]) => re.test(method)) || [null, "รูบริกประเมินตามเกณฑ์ที่ประกาศไว้"])[1];

/* ── อ่านตารางที่ 6.1 ── */
const s6 = fs.readFileSync(S6, "utf8").replaceAll("\r\n", "\n").split("## 6.1")[1].split("## 6.2")[0];
const byPlo = {};
let cur = null;
for (const row of s6.split("\n")) {
  if (!row.trim().startsWith("|")) continue;
  const c = cells(row);
  if (c.length < 8) continue;
  const head = (c[1] || "").match(/PLO(\d)/);
  if (head) cur = +head[1];
  if (!cur) continue;
  const method = clean(c[2]);
  if (!method || method.includes("รวมทุกวิธีของ") || method === "วิธีการประเมิน") continue;
  (byPlo[cur] = byPlo[cur] || []).push({
    method,
    when: clean(c[5]),
    pass: clean(c[7]),
  });
}
const n = Object.keys(byPlo).length;
if (n !== 7) throw new Error(`expected 7 PLO blocks in table 6.1, parsed ${n}`);

/* ── ประกอบตารางที่ 8.3ก ── */
const out = [
  "**ตารางที่ 8.3ก** การประเมินผลลัพธ์การเรียนรู้ระดับหลักสูตรรายข้อ",
  "",
  "| PLO | รายละเอียดผลลัพธ์การเรียนรู้ระดับหลักสูตร | วิธีวัด | เครื่องมือ | เกณฑ์ประเมิน | ระยะเวลา |",
  "|---|---|---|---|---|---|",
];
let rows = 0;
for (let p = 1; p <= 7; p++) {
  const ms = byPlo[p] || [];
  ms.forEach((m, k) => {
    const left = k === 0 ? `**PLO${p}**` : "";
    const text = k === 0 ? `${PLO_NAME[p]}<br>${PLO_TEXT[p]}` : "";
    out.push(`| ${left} | ${text} | ${m.method} | ${toolOf(m.method)} | ${m.pass} | ${m.when} |`);
    rows++;
  });
  out.push(`| | | *เกณฑ์ระดับหลักสูตร* | การสังเคราะห์ผลจากทุกวิธีข้างต้น | **ผู้เรียนผ่านเกณฑ์ทุกวิธีไม่น้อยกว่าร้อยละ 70** | ทุกสิ้นปีการศึกษา |`);
}
out.push(
  "",
  "> ตารางนี้**สร้างจากตารางที่ 6.1 ในหมวดที่ 6 โดยตรง** ด้วยคำสั่ง `npm run build:section8`",
  "> วิธีวัด เกณฑ์ประเมิน และระยะเวลา จึงตรงกับหมวดที่ 6 เสมอ · อย่าแก้ตารางนี้ด้วยมือ ให้แก้ที่ตารางที่ 6.1 แล้วสร้างใหม่",
  "",
  "> ต่างจากตารางที่ 6.1 สองประการ — ตารางนี้เพิ่มคอลัมน์ **เครื่องมือ** ซึ่งระบุแบบฟอร์มหรือรูบริกที่ใช้จริง",
  "> และไม่มีคอลัมน์น้ำหนักคะแนนกับผู้ประเมิน ซึ่งเป็นเรื่องของการตัดสินผลในหมวดที่ 6",
  "",
  "> แถวตัวเอียงท้ายแต่ละผลลัพธ์คือ**เกณฑ์ระดับหลักสูตร** ซึ่งใช้ตัดสินว่าหลักสูตรบรรลุผลลัพธ์นั้นหรือไม่",
  "> ต่างจากเกณฑ์ในแถวบนซึ่งใช้ตัดสินผู้เรียนรายบุคคล",
  "",
  "",
);

/* ── เขียนกลับเข้าไฟล์หมวดที่ 8 ── */
let s8 = fs.readFileSync(S8, "utf8").replaceAll("\r\n", "\n");
const from = s8.indexOf("**ตารางที่ 8.3ก**");
const to = s8.indexOf("## 8.4 ");
if (from < 0 || to < 0 || to < from) throw new Error("cannot locate table 8.3ก block in section 8");
s8 = s8.slice(0, from) + out.join("\n") + s8.slice(to);
fs.writeFileSync(S8, s8, "utf8");
console.log(`ตารางที่ 8.3ก: ${rows} แถววิธีวัด + 7 แถวเกณฑ์ระดับหลักสูตร · สร้างจากตารางที่ 6.1`);
